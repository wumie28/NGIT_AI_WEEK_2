def calculator (num1, num2) :
     num1 = "5"
     num2 = "3"
     sum =(5+3)
     result = sum
     print (f"The function of the code is: {sum}")



     divide = (5/3)
     result = divide
     print (f"The function of the code is : {divide}")


     subtract = (5-3)
     result = subtract
     print (f"The function of the code is {subtract}")

     multiply = (5*3)
     result = multiply
     print (f"The function of the code is : {multiply}")

    


calculator (5,3)
     

     def calculator (num1, num2) :
     if  num1 =="5":
         num2 = "3"
         sum(5+3)
         result = "sum"
         print (f"The function of the code is {sum}")

     elif divide ==(5/3):
          result = "divide"
          print (f"The function of the code is {divide}")
 
     elif subtract == (5-3) :
          result = "subtract"
          print (f"The function of the code is {subtract}")

     elif multiply == (5*3) :
          result = "multiply"
          print (f"The function of the code is {multiply}")
     else:

         print (f"The function of the code is {wrong}")  



         def  game(guess_numbers) :
      while "guess_numbers" > 0 in range (1-10) and attempt ==3 :
         if  attempt == 1 :
              print ("congratulation you won the game")
              return
         elif attempt ==2:
              print ("congratulations you won the game ")
              return
         else:
              print ("try again later")




def calculator (num1, num2) :
   try :
    
      num1= integer
      num2 =integer
      operation == (+ * / -) :
     
     
      if   num1 =="5":
            num2 = "3"
            sum(5+3)
            result = "sum"
            print (f"The function of the code is {result}")

      elif  divide ==(5/3):
          result = "divide"
          print (f"The function of the code is {result}")
 
      elif subtract == (5-3) :
          result = "subtract"
          print (f"The function of the code is {result}")

      elif multiply == (5*3) :
          result = "multiply"
          print (f"The function of the code is {result}")
      else:

          print (f"The function of the code is {invalid}") 
   except  valueError
          print (invalid error)



          def  game(guess_numbers) :
      while "guess_numbers" > 0 in range (1-10) and attempt ==3 :
         if  attempt == 1 :
              print ("congratulation you won the game")
              return
         elif attempt ==2:
              print ("congratulations you won the game ")
              return
         else:
              print ("try again later")
